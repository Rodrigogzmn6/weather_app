import 'package:flutter/material.dart';

class GraphicForecastWidget extends StatelessWidget {
  const GraphicForecastWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Graphic');
  }
}
